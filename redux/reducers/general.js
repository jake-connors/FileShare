export default (state = [], action) => {
    switch (action.type) {
        // case "SET_GENERAL_URL_PARAMS":
        //     return { ...state, urlParams: action.data };
        // case "SET_PROBABILITY_TYPES":
        //     return { ...state, probabilityTypes: action.data };
        default:
            return state;
    }
};